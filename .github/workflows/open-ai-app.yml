name: Build & deploy Next.js app to Azure Web App

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      - main
      - joscot-aar-index-new

  # Allow manual workflow trigger
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🌱 Checkout to the branch
        uses: actions/checkout@v4

      - name: 🍏 Set up Node.js version
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: ⚙️ npm install and build
        run: |
          cd ./src
          npm install
          npm run build --if-present
          cd ..

      - name: 📂 Copy standalone into the root
        run: cp -R ./src/.next/standalone ./site-deploy

      - name: 📂 Copy static into the .next folder
        run: cp -R ./src/.next/static ./site-deploy/.next/static

      - name: 📂 Copy Public folder
        run: cp -R ./src/public ./site-deploy/public

      - name: 📦 Package Next application
        run: |
          cd ./site-deploy
          zip Nextjs-site.zip ./* .next -qr

      - name: 🔍 Diagnostics
        run: |
          ls ./src
          ls ./src/.next
          ls ./site-deploy

      - name: ⬆️ Publish Next Application artifact
        uses: actions/upload-artifact@v4
        with:
          name: Nextjs-site
          path: ./site-deploy/Nextjs-site.zip

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: ${{ github.ref == 'refs/heads/main' && 'Production' || 'Beta' }}

    steps:
      - name: 🍏 Set up Node.js version
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: 📋 Display deployment environment
        run: |
          echo "Deploying to environment: ${{ github.ref == 'refs/heads/main' && 'Production' || 'Beta' }}"
          echo "Branch: ${{ github.ref }}"
          echo "App Service: tac-superchat"
          echo "Credentials configured: ${{ secrets.AZURE_CREDENTIALS != '' }}"

      - name: ⬇️ Download artifact from build job
        uses: actions/download-artifact@v4.1.8
        with:
          name: Nextjs-site

      - name: 🗝️ Azure Login
        uses: azure/login@v2.2.0
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          environment: AzureUSGovernment

      # Set the build during deployment setting to false. This setting was added in the templates to all azd to work, but breaks deployment via webapps-deploy
      - name: Azure CLI script
        uses: azure/CLI@v2.1.0
        with:
          inlineScript: |
            rg=$(az webapp list --query "[?name=='tac-superchat'].resourceGroup" --output tsv)
            echo "Resource group is $rg"
            echo "App service name is tac-superchat"
            echo Setting SCM_DO_BUILD_DURING_DEPLOYMENT=false on app service tac-superchat
            az webapp config appsettings set -n tac-superchat -g $rg --settings SCM_DO_BUILD_DURING_DEPLOYMENT=false -o none
            echo Setting --startup-file=\"node server.js\" on app service tac-superchat
            az webapp config set --startup-file="node server.js" -n tac-superchat -g $rg -o none
            sleep 10
            

      - name: 🚀 Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3.0.1
        with:
          app-name: tac-superchat
          package: ${{ github.workspace }}/Nextjs-site.zip

      - name: 🧹 Cleanup
        run: rm ${{ github.workspace }}/Nextjs-site.zip
